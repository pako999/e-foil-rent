# Neon Postgres connection string (pooled URL recommended)
DATABASE_URL="postgresql://user:password@host.neon.tech/dbname?sslmode=require"

# MailerSend transactional email
# Get an API token at https://app.mailersend.com/domains/<your-domain>/api-tokens
MAILERSEND_API_TOKEN="mlsn.xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
# FROM must be on a domain that is verified in MailerSend (use the e-foil subdomain).
MAILERSEND_FROM_EMAIL="bookings@efoil.surf-store.com"
MAILERSEND_FROM_NAME="Surf-Store E-Foil"
# Admin notifications land here.
ENQUIRY_TO_EMAIL="info@surf-store.com"

# Admin gate (header `x-admin-token` or `admin_token` cookie)
ADMIN_TOKEN="change-me-to-a-long-random-string"

# Public site URL (used by sitemap / canonical)
NEXT_PUBLIC_SITE_URL="https://efoil.surf-store.com"

# e-racuni.com proforma integration (optional — without these env vars
# the confirmation email still fires, just without a PDF attachment).
# Issue these in e-racuni → Nastavitve → API. Docs: https://e-racuni.com/si12/developer
ERACUNI_API_BASE="https://e-racuni.com/si12"
ERACUNI_API_USER=""
ERACUNI_API_PASSWORD=""
ERACUNI_API_TOKEN=""
# ERACUNI_VAT_RATE_ID="STANDARD"
